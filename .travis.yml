language: node_js
sudo: false

cache:
  directories:
    - node_modules
    - bower_components

install:
  - npm install -g grunt-cli
  - npm install -g bower
  - npm install
  - bower install

script:
  - grunt build
